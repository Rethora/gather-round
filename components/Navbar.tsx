'use client';

export default function Navbar() {
  return (
    <div className="flex-1">
      <nav className="flex justify-between w-full items-center">
        <div className="font-semibold text-lg">Logo</div>
        {/* Add any additional navbar elements here if needed */}
      </nav>
    </div>
  );
}
